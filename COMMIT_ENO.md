# Commit Eno — Project Manager

Branch: `pm/project-management`

## Commit yang dilakukan

```bash
git add .
git commit -m "chore: initialize WastePoint Laravel baseline"
git push -u origin pm/project-management
```

Setelah Pull Request baseline disetujui, merge ke `develop`. Eno kemudian membuat Issues, memeriksa Pull Request anggota, menyelesaikan konflik, menguji integrasi, dan merge `develop` ke `main`.
